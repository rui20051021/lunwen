# 快速启动测试指南 - 实时预警功能

> 本指南帮助你快速启动系统并测试实时预警功能

---

## 📋 前置条件检查

### 必需服务
- ✅ MySQL 8.0+ (端口3306)
- ✅ Redis 6.0+ (端口6379)
- ✅ Kafka 2.8+ (端口9092)
- ✅ Java 17+
- ✅ Node.js 16+
- ✅ Maven 3.6+

### 检查服务状态

```bash
# 检查MySQL
mysql -u root -p -e "SELECT VERSION();"

# 检查Redis
redis-cli ping

# 检查Kafka
# Windows
kafka-topics.bat --list --bootstrap-server localhost:9092

# Linux/Mac
kafka-topics.sh --list --bootstrap-server localhost:9092
```

---

## 🚀 启动步骤

### 步骤1: 启动基础服务

#### Windows环境

```bash
# 启动MySQL（如果未自动启动）
net start mysql80

# 启动Redis
redis-server

# 启动Zookeeper（Kafka依赖）
cd kafka安装目录
bin\windows\zookeeper-server-start.bat config\zookeeper.properties

# 启动Kafka（新开一个命令行窗口）
bin\windows\kafka-server-start.bat config\server.properties
```

#### Linux/Mac环境

```bash
# 启动MySQL
sudo systemctl start mysql

# 启动Redis
redis-server

# 启动Zookeeper
cd kafka安装目录
bin/zookeeper-server-start.sh config/zookeeper.properties &

# 启动Kafka
bin/kafka-server-start.sh config/server.properties &
```

---

### 步骤2: 创建Kafka主题

```bash
# Windows
cd kafka安装目录
bin\windows\kafka-topics.bat --create --topic sensor-temperature --bootstrap-server localhost:9092 --partitions 3 --replication-factor 1
bin\windows\kafka-topics.bat --create --topic sensor-gps --bootstrap-server localhost:9092 --partitions 3 --replication-factor 1

# Linux/Mac
cd kafka安装目录
bin/kafka-topics.sh --create --topic sensor-temperature --bootstrap-server localhost:9092 --partitions 3 --replication-factor 1
bin/kafka-topics.sh --create --topic sensor-gps --bootstrap-server localhost:9092 --partitions 3 --replication-factor 1

# 验证主题创建成功
# Windows
bin\windows\kafka-topics.bat --list --bootstrap-server localhost:9092

# Linux/Mac
bin/kafka-topics.sh --list --bootstrap-server localhost:9092
```

---

### 步骤3: 初始化数据库

```bash
# 进入数据库目录
cd database

# Windows
connect.bat

# Linux/Mac
mysql -u root -p freshlogistics < schema.sql
mysql -u root -p freshlogistics < init_data.sql
```

**确保以下表存在**:
- `alert_records` - 预警记录表
- `transports` - 运输任务表
- `vehicles` - 车辆表
- `sensor_data` - 传感器数据表
- `orders` - 订单表
- `products` - 产品表

---

### 步骤4: 启动后端服务

```bash
cd backend

# 方式1: 使用启动脚本（推荐）
# Windows
start-backend.bat

# Linux/Mac
./start-backend.sh

# 方式2: 使用Maven命令
mvn spring-boot:run

# 方式3: 使用JAR包
mvn clean package
java -jar target/fresh-logistics-backend-1.0.0.jar
```

**启动成功标志**:
```
Started FreshLogisticsSimpleApplication in X.XXX seconds
WebSocket配置已加载
Kafka消费者已启动
定时任务已启动
```

**访问后端**:
- API地址: http://localhost:8080/api
- 健康检查: http://localhost:8080/api/actuator/health

---

### 步骤5: 启动前端服务

```bash
cd frontend

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

**启动成功标志**:
```
VITE v5.x.x  ready in XXX ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

**访问前端**: http://localhost:3000

---

## 🧪 功能测试

### 测试1: WebSocket连接测试

#### 1.1 登录系统
1. 访问 http://localhost:3000
2. 使用任意账号登录（如: admin / admin123）
3. 打开浏览器开发者工具（F12）
4. 查看Console标签

**预期输出**:
```
正在连接WebSocket: ws://localhost:8080/ws/alerts?userId=1
WebSocket连接成功
```

#### 1.2 检查连接状态
在浏览器Console中执行:
```javascript
// 检查WebSocket连接状态
websocketClient.isConnected()
// 应该返回: true

// 获取在线用户数（需要后端支持）
websocketClient.getOnlineUserCount()
```

---

### 测试2: 温控预警测试

#### 2.1 准备测试数据

在数据库中创建测试数据：

```sql
-- 1. 创建测试产品（需要温控）
INSERT INTO products (product_name, product_type, min_temperature, max_temperature, created_at)
VALUES ('新鲜蔬菜', 'vegetable', 0, 10, NOW());

-- 2. 创建测试订单
INSERT INTO orders (order_code, supplier_id, purchaser_id, order_status, created_at)
VALUES ('TEST001', 1, 2, 'confirmed', NOW());

-- 3. 创建订单明细
INSERT INTO order_items (order_id, product_id, quantity, unit_price)
VALUES (LAST_INSERT_ID(), 1, 100, 5.00);

-- 4. 创建运输任务
INSERT INTO transports (transport_code, order_id, vehicle_id, driver_id, route_id, 
                       transport_status, actual_start_time, planned_arrival_time)
VALUES ('T001', LAST_INSERT_ID(), 1, 1, 1, 'in_transit', NOW(), DATE_ADD(NOW(), INTERVAL 2 HOUR));
```

#### 2.2 发送温度异常数据

使用Postman或curl发送请求：

```bash
# 测试温度过高（应该触发预警）
curl -X POST "http://localhost:8080/api/simulator/temperature-abnormal?vehicleId=1&abnormalType=high"

# 测试温度过低（应该触发预警）
curl -X POST "http://localhost:8080/api/simulator/temperature-abnormal?vehicleId=1&abnormalType=low"

# 测试正常温度（不应该触发预警）
curl -X POST "http://localhost:8080/api/simulator/temperature?vehicleId=1&temperature=5.0"
```

#### 2.3 验证预警

**前端验证**:
- ✅ 右上角弹出红色预警通知
- ✅ 通知标题: "温控异常预警"
- ✅ 通知内容包含: 运输任务编号、产品名称、当前温度
- ✅ 通知不会自动关闭（duration: 0）
- ✅ 点击通知可跳转到预警详情

**后端验证**:
```sql
-- 查询预警记录
SELECT * FROM alert_records 
WHERE alert_type = 'temperature' 
ORDER BY created_at DESC 
LIMIT 5;
```

**日志验证**:
查看后端日志，应该看到：
```
收到温度传感器数据: {...}
温控预警已触发 - 运输任务: T001, 原因: 温度过高：当前 28.5℃，要求 ≤ 10.0℃
预警消息已推送 - 用户ID: 1, 预警类型: temperature
```

---

### 测试3: 超时预警测试

#### 3.1 创建超时运输任务

```sql
-- 创建一个已经超时的运输任务
INSERT INTO transports (transport_code, order_id, vehicle_id, driver_id, route_id,
                       transport_status, actual_start_time, planned_arrival_time)
VALUES ('T002', 1, 1, 1, 1, 'in_transit', 
        DATE_SUB(NOW(), INTERVAL 3 HOUR),  -- 3小时前出发
        DATE_SUB(NOW(), INTERVAL 1 HOUR)); -- 1小时前应该到达

-- 设置路线的最大延迟容忍时长为30分钟
UPDATE transport_routes 
SET max_delay_tolerance = 30 
WHERE id = 1;
```

#### 3.2 等待定时任务触发

超时预警由定时任务检测，每分钟执行一次。

**等待时间**: 最多1分钟

#### 3.3 验证预警

**前端验证**:
- ✅ 右上角弹出红色预警通知
- ✅ 通知标题: "运输超时预警"
- ✅ 通知内容包含: 运输任务编号、延迟时长

**后端日志**:
```
开始检查超时预警...
超时预警已触发 - 运输任务: T002, 延迟: 90 分钟
预警消息已推送 - 用户ID: 1, 预警类型: timeout
超时预警检查完成 - 检查了 2 个运输任务
```

---

### 测试4: 路径偏离预警测试

#### 4.1 准备测试数据

```sql
-- 设置路线的预设路径和偏离阈值
UPDATE transport_routes 
SET route_points = '[{"lat":39.9,"lng":116.4},{"lat":39.95,"lng":116.45}]',
    deviation_threshold = 500  -- 500米
WHERE id = 1;
```

#### 4.2 发送GPS偏离数据

```bash
# 发送偏离路线的GPS数据
curl -X POST "http://localhost:8080/api/simulator/gps?vehicleId=1&latitude=40.5&longitude=117.0"
```

#### 4.3 验证预警

**前端验证**:
- ✅ 右上角弹出橙色预警通知
- ✅ 通知标题: "路径偏离预警"
- ✅ 通知内容包含: 运输任务编号、路线名称、偏离距离

---

### 测试5: 预警处理测试

#### 5.1 查看预警列表

1. 登录系统
2. 进入"预警管理"页面
3. 查看待处理预警列表

#### 5.2 开始处理预警

1. 点击某条预警的"开始处理"按钮
2. 填写处理说明，如: "正在联系司机确认情况"
3. 点击"确定"

**验证**:
```sql
-- 查询预警状态
SELECT alert_code, alert_status, processor_id, process_notes 
FROM alert_records 
WHERE id = 1;

-- 预期结果:
-- alert_status: processing
-- processor_id: 当前用户ID
-- process_notes: 正在联系司机确认情况
```

#### 5.3 完成预警处理

1. 点击"完成处理"按钮
2. 填写处理结果，如: "已联系司机，温控设备已修复"
3. 点击"确定"

**验证**:
```sql
-- 查询预警状态
SELECT alert_code, alert_status, process_result 
FROM alert_records 
WHERE id = 1;

-- 预期结果:
-- alert_status: processed
-- process_result: 已联系司机，温控设备已修复
```

---

### 测试6: 批量压力测试

#### 6.1 发送批量数据

```bash
# 发送1000条传感器数据
curl -X POST "http://localhost:8080/api/simulator/batch?count=1000&vehicleIdStart=1"
```

#### 6.2 监控指标

**后端监控**:
- 查看CPU使用率
- 查看内存使用率
- 查看Kafka消费速度

**前端监控**:
- 查看预警通知数量
- 查看页面响应速度
- 查看WebSocket连接状态

**数据库监控**:
```sql
-- 查询传感器数据条数
SELECT COUNT(*) FROM sensor_data 
WHERE created_at > DATE_SUB(NOW(), INTERVAL 1 MINUTE);

-- 查询预警记录条数
SELECT COUNT(*) FROM alert_records 
WHERE created_at > DATE_SUB(NOW(), INTERVAL 1 MINUTE);
```

---

## 🐛 常见问题排查

### 问题1: WebSocket连接失败

**症状**: 前端Console显示连接失败

**排查步骤**:
1. 检查后端是否启动
   ```bash
   curl http://localhost:8080/api/actuator/health
   ```

2. 检查WebSocket端点是否可访问
   - 使用WebSocket测试工具连接: `ws://localhost:8080/ws/alerts?userId=1`

3. 查看后端日志
   ```
   tail -f backend/logs/application.log
   ```

4. 检查防火墙设置

**解决方案**:
- 确保后端已启动
- 确保WebSocketConfig配置正确
- 检查CORS设置

---

### 问题2: 预警未触发

**症状**: 发送传感器数据后没有收到预警

**排查步骤**:
1. 检查Kafka是否正常运行
   ```bash
   # Windows
   bin\windows\kafka-topics.bat --list --bootstrap-server localhost:9092
   
   # Linux/Mac
   bin/kafka-topics.sh --list --bootstrap-server localhost:9092
   ```

2. 检查Kafka消费者是否启动
   - 查看后端日志，应该看到: "Kafka消费者已启动"

3. 检查数据库中是否有对应的运输任务
   ```sql
   SELECT * FROM transports WHERE vehicle_id = 1 AND transport_status = 'in_transit';
   ```

4. 检查预警规则是否配置
   ```sql
   SELECT * FROM products WHERE id = 1;
   -- 确保 min_temperature 和 max_temperature 有值
   ```

5. 查看后端日志
   ```
   grep "温控预警" backend/logs/application.log
   ```

**解决方案**:
- 确保Kafka正常运行
- 确保有在途运输任务
- 确保产品配置了温控规则

---

### 问题3: 定时任务未执行

**症状**: 超时预警未自动触发

**排查步骤**:
1. 检查@EnableScheduling注解
   - 确认SchedulingConfig.java存在

2. 查看后端日志
   ```
   grep "开始检查超时预警" backend/logs/application.log
   ```

3. 检查定时任务配置
   ```yaml
   # application.yml
   spring:
     task:
       scheduling:
         pool:
           size: 5
   ```

**解决方案**:
- 确保SchedulingConfig配置正确
- 重启后端服务

---

### 问题4: 预警重复触发

**症状**: 同一个预警多次触发

**排查步骤**:
1. 检查防重复逻辑
   ```sql
   SELECT * FROM alert_records 
   WHERE related_id = 1 AND alert_type = 'temperature'
   ORDER BY created_at DESC;
   ```

2. 查看后端日志

**解决方案**:
- 温控预警: 5分钟内不重复
- 路径偏离预警: 10分钟内不重复
- 超时预警: 已触发的不再触发

---

## 📊 性能测试

### 测试预警响应延迟

**目标**: ≤ 3秒

**测试方法**:
1. 记录发送时间
2. 发送温度异常数据
3. 记录前端收到预警的时间
4. 计算时间差

**测试脚本**:
```javascript
// 在浏览器Console中执行
const startTime = Date.now();

// 发送请求
fetch('http://localhost:8080/api/simulator/temperature-abnormal?vehicleId=1&abnormalType=high', {
  method: 'POST'
});

// 监听预警事件
websocketClient.on('alert', (alert) => {
  const endTime = Date.now();
  const delay = endTime - startTime;
  console.log(`预警响应延迟: ${delay}ms`);
});
```

---

## ✅ 测试检查清单

### 基础功能
- [ ] WebSocket连接成功
- [ ] 用户登录后自动连接
- [ ] 心跳保活正常
- [ ] 断线自动重连

### 预警触发
- [ ] 温控预警正常触发
- [ ] 超时预警正常触发
- [ ] 路径偏离预警正常触发
- [ ] 预警不重复触发

### 预警推送
- [ ] 前端收到预警通知
- [ ] 通知内容正确
- [ ] 通知级别正确（颜色）
- [ ] 点击通知可跳转

### 预警处理
- [ ] 可以开始处理预警
- [ ] 可以完成预警处理
- [ ] 可以忽略预警
- [ ] 状态更新正确

### 性能指标
- [ ] 预警响应延迟 ≤ 3秒
- [ ] 支持1000+并发连接
- [ ] 数据处理吞吐量 ≥ 5万条/秒

---

## 📞 技术支持

如果遇到问题，请：
1. 查看本指南的"常见问题排查"部分
2. 查看后端日志: `backend/logs/application.log`
3. 查看前端Console错误信息
4. 查看数据库数据是否正确

---

**文档版本**: v1.0.0  
**最后更新**: 2025-12-29
